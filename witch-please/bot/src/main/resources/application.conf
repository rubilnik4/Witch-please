project {
  host = "0.0.0.0"
  port = 8081
  tarotUrl = "http://localhost:8080"
  userSecretPepper = ${?USER_SECRET_PEPPER}
  minFutureTime = "10 minutes"
  maxFutureTime = "90 days"
}

telegram {
  token = ${?TELEGRAM_BOT_TOKEN}
  secret = ${?TELEGRAM_BOT_SECRET}
  baseUrl = "https://82thcwmf-8081.euw.devtunnels.ms/"
}

telemetry {
  appName = "witch-bot"
  otelEndpoint = "http://localhost:4317"
  prometheusPort = 9464
  logLevel = "DEBUG"
}

localStorage {
  path = "/tmp/bot"
}