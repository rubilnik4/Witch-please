project {
  host = "0.0.0.0"
  port = 8081
  tarotUrl = "http://localhost:8080"
  userSecretPepper = ${?USER_SECRET_PEPPER}
  maxPastTime = "5 minutes"
  maxFutureTime = "90 days"
  maxCardOfDayDelay = "8 hours"
}

telegram {
  token = ${?TELEGRAM_BOT_TOKEN}
  secret = ${?TELEGRAM_BOT_SECRET}
  baseUrl = "https://s12mkv4s-8081.euw.devtunnels.ms/"
}

telemetry {
  appName = "witch-bot"
  otelEndpoint = ${?OTEL_EXPORTER_OTLP_ENDPOINT}
  logLevel = "DEBUG"
}

localStorage {
  path = "/tmp/witch-bot"
}
