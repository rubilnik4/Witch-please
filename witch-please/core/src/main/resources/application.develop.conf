publish {
  maxPastTime = "5 minutes"
}

jwt {
  secret = "12345678"
}

postgres {
  connectionString = "jdbc:postgresql://localhost:5432/postgres?user=postgres&password=postgres"
}

telemetry {
  otelEndpoint = "http://localhost:4317"
}

storage {
  type = "local"

  local {
    path = "/tmp/witch-please"
  }
}
